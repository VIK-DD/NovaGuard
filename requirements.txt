# Upper bounds cap surprise major upgrades (supply-chain / breaking changes).
# For fully reproducible, hash-verified installs, regenerate the universal lock:
#   uv pip compile --universal --generate-hashes --python-version 3.11 -o requirements.lock requirements.txt
discord.py>=2.7.1,<3
# 3.14.3 fixes CVE-2026-69244 plus the websocket issues fixed in 3.14.2.
aiohttp>=3.14.3,<4
anthropic>=1.0.0,<2
# Floor is 50.0: earlier releases carry CVE-2026-69247, and the old <50
# ceiling pinned us to exactly the vulnerable 49.x. Used for Fernet
# encryption of OAuth tokens at rest in core/web_storage.py.
cryptography>=50.0,<51
