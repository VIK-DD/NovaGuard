# Upper bounds cap surprise major upgrades (supply-chain / breaking changes).
# For fully reproducible, hash-verified installs, generate a lock on the Pi:
#   .venv/bin/pip freeze > requirements.lock   (then install from the lock)
discord.py>=2.7.1,<3
aiohttp>=3.9,<4
anthropic>=0.40,<1
# Floor is 50.0: earlier releases carry CVE-2026-69247, and the old <50
# ceiling pinned us to exactly the vulnerable 49.x. Used for Fernet
# encryption of OAuth tokens at rest in core/webserver.py.
cryptography>=50.0,<51

# Music. yt-dlp resolves and streams YouTube/SoundCloud. PyNaCl and davey
# are discord.py's voice backends. yt-dlp breaks whenever YouTube changes
# something, so expect to bump it more often than the rest.
yt-dlp[default]>=2026.7.0
PyNaCl>=1.5,<2
davey>=0.1.6,<0.2
