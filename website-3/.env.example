# Copy to .env for local dev. Bot API origin, no trailing slash.
# Production example: https://api.novaguard.fun (the HTTPS Cloudflare Tunnel
# hostname that points to the bot's local port 8300).
PUBLIC_API_BASE=http://localhost:8300
